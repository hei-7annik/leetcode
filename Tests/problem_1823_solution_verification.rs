mod test {
    use leetcode::problem_1823::find_the_winner::{find_the_winner};

    mod naive_solution {
        use super::*;
        #[test]
        fn with_() {
            assert_eq!(find_the_winner(), )
        }

        #[test]
        fn with_() {
            assert_eq!(find_the_winner(), )
        }
    }

    mod optimized_solution {
        use super::*;
    }
}